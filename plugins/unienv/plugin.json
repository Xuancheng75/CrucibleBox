{
  "name": "unienv",
  "version": "0.10.0",
  "displayName": "UniEnv",
  "description": "一键式开发环境配置工具 — 支持 Python、Node.js、Git、Go、Java、Rust、PHP、Ruby、Zig、Deno、Bun 的版本管理、安装后环境配置、切换与卸载",
  "author": "UniEnv Team",
  "main": "dist/main.js",
  "renderer": "dist/renderer.js",
  "manifestVersion": 2,
  "backendApiVersion": 2,
  "rendererApiVersion": 2,
  "permissions": ["trusted:unienv"],
  "config": {
    "installRoot": {
      "type": "string",
      "label": "安装根目录",
      "description": "所有工具将安装在此目录下，默认为 C:\\UniEnv",
      "default": "C:\\UniEnv"
    },
    "downloadMirror": {
      "type": "select",
      "label": "下载镜像",
      "description": "选择下载源，国内用户建议选择镜像以获得更快速度",
      "default": "direct",
      "options": [
        { "label": "官方源 (国际)", "value": "direct" },
        { "label": "华为云 (国内)", "value": "huawei" },
        { "label": "阿里云 (国内)", "value": "aliyun" },
        { "label": "TUNA 清华 (国内)", "value": "tuna" }
      ]
    },
    "onlineVersions": {
      "type": "select",
      "label": "联网检查语言版本",
      "description": "开启后 Node/Go/Java/Ruby/Zig/Deno/Bun 会从官方源发现新版本并可直接安装（Python/Git 暂不支持在线新版本）；关闭则仅显示内置版本目录。网络不可达时自动回退内置目录。",
      "default": "on",
      "options": [
        { "label": "开启（推荐）", "value": "on" },
        { "label": "关闭", "value": "off" }
      ]
    },
    "autoConfigureEnvironment": {
      "type": "select",
      "label": "安装后自动配置环境",
      "description": "安装或组合包完成后，将 UniEnv 的当前版本入口加入当前用户 PATH；可随时关闭并恢复原环境。",
      "default": "on",
      "options": [
        { "label": "开启（推荐）", "value": "on" },
        { "label": "仅安装，不修改环境", "value": "off" }
      ]
    },
    "customCombos": {
      "type": "string",
      "label": "自定义组合包",
      "description": "JSON 格式的自定义组合包配置",
      "default": "[]"
    }
  }
}
