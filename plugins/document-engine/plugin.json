{
  "name": "document-engine",
  "version": "0.9.0",
  "displayName": "Document Engine",
  "description": "本地文档处理基础设施：支持最多 2000 页 PDF 的 OCR、解析结果导出、真实 PDF 拆分、AI/RAG 文本分块、格式转换、批量任务、缓存和模型管理",
  "author": "CrucibleBox",
  "main": "dist/main.js",
  "renderer": "dist/renderer.js",
  "manifestVersion": 2,
  "backendApiVersion": 2,
  "rendererApiVersion": 2,
  "minHostVersion": "2.0.0-beta.8",
  "permissions": ["trusted:document-engine", "dialog"],
  "config": {
    "modelDirectory": {
      "type": "string",
      "label": "OCR 模型目录",
      "description": "PaddleOCR ONNX 模型与字典目录；留空使用内置轻量模型",
      "default": ""
    },
    "modelProfile": {
      "type": "select",
      "label": "OCR 模型方案",
      "description": "默认使用 PP-OCRv6-small-det + 通用 PP-OCRv5_mobile_rec；英文模型仅在明确选择时使用",
      "default": "auto",
      "options": [
        { "label": "自动（推荐）", "value": "auto" },
        { "label": "PP-OCRv5 中英混排方案", "value": "ppocrv6-small-det-v5-mobile-rec" },
        { "label": "PP-OCRv5 英文优化方案", "value": "ppocrv6-small-det-v5-en-rec" },
        { "label": "PP-OCRv4 兼容方案", "value": "ppocrv4-mobile-zh-en" }
      ]
    },
    "textRecognitionMode": {
      "type": "select",
      "label": "文字识别语言",
      "description": "自动/中文混排使用通用 PP-OCRv5；英文优化模式需明确选择",
      "default": "auto",
      "options": [
        { "label": "自动（中英混排）", "value": "auto" },
        { "label": "中文/混排", "value": "mixed" },
        { "label": "英文优化", "value": "english" }
      ]
    },
    "dictionaryPath": {
      "type": "string",
      "label": "OCR 字典路径",
      "description": "识别字典路径；留空根据模型方案自动选择",
      "default": ""
    },
    "cacheDirectory": {
      "type": "string",
      "label": "缓存目录",
      "description": "处理结果缓存目录（基于 source hash + engine + options）",
      "default": ""
    },
    "outputDirectory": {
      "type": "string",
      "label": "输出目录",
      "description": "默认导出 / 转换输出目录",
      "default": ""
    },
    "device": {
      "type": "select",
      "label": "计算设备",
      "description": "自动模式优先使用 Windows DirectML（NVIDIA GPU），不可用时回退 CPU",
      "default": "auto",
      "options": [
        { "label": "自动 (Auto)", "value": "auto" },
        { "label": "CPU", "value": "cpu" },
        { "label": "GPU (DirectML)", "value": "gpu" }
      ]
    }
  }
}
