# 2.1.0-beta.1 开发进度

- 状态：PAUSED_AFTER_B13
- 当前工作包：B13（本地验收完成，发布暂停）
- 当前分支：`codex/beta8-download-workspace`
- 基线提交：`b38133d4eedfd793a1c2b2500f7e097391371526`
- 最后更新：2026-09-18 Asia/Shanghai
- 当前负责人：Codex
- 下一步唯一动作：等待用户明确授权提交或发布
- 下一条建议命令：无；不生成安装包、更新资产或发布制品
- 阻塞原因：无

## 执行约束

- 不提交、不推送 GitHub，直到用户另行授权。
- 保留开始执行前已有的所有工作树修改。
- 不新增 SHA-256、哈希校验或类似防御性编程。
- 快速解压在合并前必须先补齐未完成能力。

## 备份

- [x] B00-01 保存完整工作区快照（排除 `.git`、依赖缓存和构建产物）
- [x] B00-02 保存 tracked binary diff、Git 状态和基线提交
- 位置：`E:\OCR\CrucibleBox-backup-20260917-143812`
- 快照：`workspace-snapshot.zip`
- tracked diff：`tracked-working-tree.patch`
- 开始执行时工作区已有多项未提交修改和未跟踪文件；均视为用户资产。

## PDF 回归样本

- [x] 已发现 `C:\Users\hjc\Desktop\fogharbor_botanical_field_notes_scanned.pdf`
- [x] 已发现 `C:\Users\hjc\Desktop\linear algebra by strang 4 th edition.pdf`
- [x] 已发现 `C:\Users\hjc\Desktop\WordCountProject\78_361002200708112019_24.pdf`
- [x] 完成三份文件的真实解析、代表页渲染和 OCR/RAG 回归
- [x] 回归结果记录在本文 B13；临时渲染图验收后已清理

## B00 基线、版本和性能

- 状态：IN_PROGRESS
- 分支：`codex/beta8-download-workspace`
- 开始提交：`b38133d4eedfd793a1c2b2500f7e097391371526`
- 完成提交：未提交

### TODO

- [x] B00-01 创建恢复备份
- [x] B00-02 创建完整计划与实时进度文档
- [x] B00-03 运行 Tauri 前端构建并记录错误
- [x] B00-04 运行 Rust fmt check、clippy和 workspace tests
- [x] B00-05 运行现有插件测试与构建基线（282 项非 Electron 测试通过；冻结 Electron 套件因本机未安装 Electron binary 单独留待 B02 删除）
- [x] B00-06 修复当前前端入口构建问题
- [x] B00-07 将版本对齐到 `2.1.0-beta.1`
- [x] B00-08 验证版本对齐脚本覆盖七个 Tauri 版本点
- [x] B00-09 建立前端产物体积基线
- [x] B00-10 记录首轮性能基线

### 实际修改

- 新增 `docs/2.1.0-beta.1-plan.md`。
- 新增本进度文档。
- 删除 `App.tsx` 对 `StartupUpdatePrompt` 的重复静态导入，保留懒加载入口，修复 `no-import-assign`。
- Tauri 主程序、sidecar、前端及两个 lockfile 已统一为 `2.1.0-beta.1`。
- 修复 Rust 1.98 新增 clippy 规则触发的六处切片遍历写法。
- 快速解压改用官方 7-Zip Extra 26.03 x64 `7za` 运行组件；准备脚本使用官网直链、超时和重试，不进行 SHA-256/哈希校验。
- CI 与 Tauri 发布流程在 Rust 构建前准备 7-Zip 资源。
- 快速解压插件纳入生产插件清单测试，并同步统一 renderer 构建器。

### 验证证据

- 备份命令成功，快照大小约 231 MiB。
- `tauri-frontend npm run build` 通过；主入口 chunk 607.05 kB（gzip 195.98 kB），触发 500 kB 告警。
- `cargo fmt -- --check` 通过。
- Rust clippy/test 首轮均被 `binaries/cruciblebox-plugin-host-x86_64-pc-windows-gnu.exe` 缺失阻断；现有 staged 文件只有 MSVC triple 名称，属于本机工具链与资产命名不一致。
- 全部 12 个现有插件（含快速解压）完成构建；整仓 `npm run check` 在 lint 阶段发现并定位 `StartupUpdatePrompt` 重复导入。
- PDF 基线：扫描样本 10 页 A4；线性代数教材 542 页、382 x 510.68 pt；业务 PDF 1 页 A4。
- GNU triple 本地 sidecar 文件补齐后，`cargo clippy --workspace --all-targets --locked -- -D warnings` 通过。
- `cargo test --workspace --locked`：252 passed、0 failed、3 ignored。
- 版本对齐脚本通过：Tauri `2.1.0-beta.1` 在七个版本点一致；根 Electron 遗留包仍保持 1.7.3。
- 快速解压运行时自检通过；含中文文件名的临时 ZIP 已完成真实解压并核对内容。
- 整仓格式、lint、TypeScript 与 282 项非 Electron 测试通过；唯一失败套件为冻结 Electron `pluginBackendRpc.test.ts`，原因是本机 Electron binary 缺失且下载失败。

### 未解决问题

- 当前工作树包含开始执行前的未提交修改，实施时必须逐文件避让。
- 当前分支名称仍为旧 beta8 工作分支，用户要求不提交，因此暂不切换或创建提交。
- 根 `npm run check` 仍被冻结 Electron 测试套件拖累；B02 删除 Electron 活依赖后应彻底消除此阻断，不为旧运行线重新下载安装二进制。

### 接力说明

- 当前完成到：B00 基线、版本对齐、快速解压运行时准备和首个真实解压冒烟。
- 工作区未提交文件：以 `git status --short` 为准；包含用户原有修改和本次新增文档。
- 下一步：进入 B01，统一宿主声明/实现并增加契约测试；随后 B02 解耦 Electron。
- 注意事项：不得重置工作树；不得删除未跟踪的 `archive-extractor`、`poc-ocr` 等现有目录。

## B01 宿主 RPC 与权限

- 状态：COMPLETE
- [x] 修复声明与实现断层
- [x] 实现真实事件和快捷键
- [x] 统一文件二进制协议
- [x] 增加结构化错误和一致性测试

### B01 实际修改

- 共享 RPC 白名单补入 `clipboard.read`、`clipboard.write`、`system.info`，并补齐参数校验。
- 宿主实现真实文件/目录选择、全局快捷键注册与注销、剪贴板读写和系统信息读取。
- 插件事件从单纯确认改为宿主广播：`event.emit` 会投递给所有已激活 backend，由 sidecar 内的订阅表分发；`event.subscribe/unsubscribe` 管理插件处理器。
- 快捷键按下时同时向前端发布 `plugin:shortcut`，并向 backend 投递对应宿主事件，使注册的插件处理器能够执行。
- `file.write` 统一使用 base64 信封；sidecar 会把字符串或 `Uint8Array` 编码后再交给宿主落盘。
- host 错误不再一律伪装为 `NOT_ALLOWED`，现区分 `NOT_ALLOWED`、`TIMEOUT`、`INVALID_MESSAGE`、`INTERNAL_ERROR`。
- `dialog.open`、快捷键方法加入实际实现面，可信服务权限按服务名精确匹配。

### B01 核查记录

- Rust 格式与静态检查曾通过；Rust 测试执行到 251 通过、3 忽略，发现并修正 beta 版本下稳定市场 URL 测试的期望值。
- 新增 sidecar base64 编码回归（含英文与中文文本）。
- 随后用户明确要求停止 SHA-256、哈希、门禁、冒烟等防御性检查；已立即中断后续整套检查，未再继续。
- 未提交、未推送 GitHub。

## B02 Electron 解耦与删除

- 状态：COMPLETE
- [x] 迁移 UniEnv 活依赖
- [x] 清理根脚本和依赖
- [x] 删除 Electron CI、配置和脚本
- [x] 删除三个冻结目录

### B02 实际修改

- UniEnv renderer 任务类型迁入插件自身，不再从 `plugin-system` 导入运行期类型。
- 根 npm 开发、构建、预览和发布入口改为 Tauri；移除 Electron、electron-builder、electron-vite、electron-updater、fuses、rebuild 和 better-sqlite3 依赖。
- 活动 CI 只保留 Tauri 验证与发布链；删除旧 Electron 发布工作流、构建配置和脚本。
- `electron/`、`database/`、`plugin-system/` 已从活动工作树移除；历史实现继续由冻结 tag、开发备份和 `docs/history/` 承载。
- 旧 Electron/UniEnv TypeScript 测试不再进入活动 TypeScript 与 Vitest 配置；由于删除这些历史测试文件的操作被环境安全策略拒绝，文件本身保留为不可执行的历史材料。

## B03 统一网络与代理

- 状态：COMPLETE
- [x] 建立统一网络策略、客户端和诊断
- [x] 迁移更新、市场、插件、环境和模型下载
- [x] 增加代理格式检查、连接诊断和脱敏路由信息

### B03 实际修改

- 新增宿主唯一网络策略：`direct` 禁用代理；`system` 使用系统代理；`manual` 使用指定 HTTP/HTTPS/SOCKS5；`auto` 优先有效手动地址，否则回到系统代理。
- 设置保存后立即刷新宿主网络策略，不再出现页面显示已切换但下载继续使用旧客户端的情况。
- 插件市场目录与下载、应用更新、UniEnv 版本发现与制品下载、文档模型下载、插件 `network.fetch` 已切换到同一策略。
- 设置页新增 DNS、TCP、TLS、插件市场和更新服务的分阶段网络诊断，显示实际路由、耗时和自动回退原因；路由中的用户名与密码会脱敏。
- 市场目录缓存按网络路由隔离；市场与更新结果携带实际网络路由。
- 按用户“不做防御性编程”的要求，未增加凭据库、哈希或额外门禁；市场下载路径已移除 SHA-256 计算与比较。

## B04 统一任务与市场

- 状态：COMPLETE
- [x] 增加 `paused` 和 `waiting-user`
- [x] 合并市场下载和安装任务
- [x] 删除右下角常驻浮层
- [x] 市场任务使用宿主事件推送

### B04 实际修改

- 任务状态统一为 `queued/running/paused/waiting-user/completed/failed/cancelled`，任务中心补齐中文状态。
- 单个市场任务 ID 贯穿下载、准备、等待确认、安装、完成或取消；不再在安装预览阶段另建任务。
- 批量市场下载保留每个插件自己的任务 ID，并把下载结果按原任务送入安装确认队列。
- 宿主通过 `host:task` 推送市场进度；前端仅接收事件更新 zustand，市场卡片显示自身进度。
- 删除 `GlobalTaskDock` 及右下角固定浮层样式；顶栏改为带数量徽标的紧凑任务入口。
- 任务中心支持取消进行中的市场下载；宿主停止 Range 下载并删除临时部分文件，任务进入 `cancelled`。
- 安装预览阶段使用 `waiting-user`，确认后继续同一任务，取消确认后同一任务终结。

### B02-B04 执行说明

- 遵照用户要求，没有执行 SHA-256、哈希校验、质量门禁或冒烟测试。
- 没有提交或推送 GitHub。

## B05 性能优化

- 状态：COMPLETE
- [x] 页面离开后卸载，插件页面保持单实例
- [x] 插件列表请求合并
- [x] 系统动态信息限频缓存
- [x] 网络与文件操作不再各自创建代理策略

### B05 实际修改

- 前端不再把所有访问过的页面永久隐藏保活；当前只挂载正在显示的普通页面或插件页面，离开即释放页面定时器、列表和大对象。
- 插件列表并发读取合并为一个共享 Promise，避免首页、市场和安装完成同时重复读取数据库。
- 系统信息采样增加两秒缓存，避免多个插件同时刷新 CPU、内存、磁盘和网络信息。
- B03 的单一网络客户端同时消除了多个下载路径重复创建、配置不一致的问题。
- 未执行性能门禁或基准冒烟；本轮只落实代码级性能改造。

## B06 Plugin SDK v3

- 状态：COMPLETE
- [x] Manifest/API v3
- [x] 标准能力声明
- [x] 高信任能力和安装确认
- [x] v2运行兼容

### B06 实际修改

- Manifest 支持 v3，以及 `minimumHostVersion`、`trustLevel`、`capabilities`；继续读取 v2 的 `minHostVersion` 和权限数组。
- 能力集合覆盖 `storage/fs/network/process/archive/tasks/events/ui/system/crypto/credentials/pluginData`；已实现能力自动映射为现有细粒度权限。
- `trustLevel: full` 自动加入 `host:full-trust`；权限守卫将其作为完整宿主授权，安装/升级弹窗显示独立高风险提示。
- SDK 新增 `ctx.pluginData` 和 `ctx.capabilities.events/system`，原 `ctx.storage`、`ctx.api` 保持不变。
- Manifest v3 renderer 复用稳定的 v2 帧协议，现有 v2 插件无需修改。
- `docs/plugin-sdk.md` 已更新为 v3 主规范。

## B07 文档与知识库

- 状态：COMPLETE（本轮不执行样本回归和冒烟）
- [x] 综合文件工作台
- [x] PDF 拆分与结构化解析
- [x] Office、网页、数据、邮件和 EPUB 内容提取
- [x] OCR、版面、表格、公式和质量结果
- [x] RAG 分块与语料导出
- [x] 批量自动化、任务、历史、模型与缓存

### B07 实际修改

- 插件用户名称统一为“文档与知识库”，版本更新为 `0.11.0-beta.1`，采用 Manifest v3；市场、拖放提示和插件界面不再出现抽象的 Engine 名称。
- 导航固定为：文件工作台、PDF 工具、格式转换、文字识别、内容提取、知识库预处理、自动化流程、任务与质量、模型与缓存。
- 输入在现有 PDF、图片、TXT、Markdown、HTML、DOCX、PPTX、XLSX 基础上增加 JSON、XML、CSV、TSV、EML 和 EPUB。
- EPUB 按正文 HTML/XHTML 顺序生成页面；JSON 格式化后进入统一 Document；邮件提取正文；结构化数据保留原始行列文本供后续分块。
- 复用并整理现有专业底座：PDF 原生文字与扫描页分流、OCR、页面/章节/范围拆分、章节树、页眉页脚清理、表格/公式/图片元数据、Markdown/JSON/DOCX/PDF 导出、混合 RAG 分块、批处理、暂停/继续/取消/重试、质量报告和缓存。
- 远程自定义 OCR 模型安装不再要求用户填写 SHA-256；界面删除相关输入和校验文案，遵守“不做哈希防御性编程”的要求。
- 桌面三份 PDF 仍保留为后续人工验收样本，但本轮按用户要求没有运行回归、渲染或冒烟。
- 快速解压继续作为独立的中文“快速解压”能力存在；其后续创建归档、密码和分卷功能不在本轮 B07 文档插件改造中伪装为已完成。

### B05-B07 执行说明

- 没有运行 SHA-256、哈希校验、质量门禁、构建测试、性能基准或冒烟测试。
- 没有提交或推送 GitHub。

## B08 开发环境管理

- 状态：COMPLETE（未运行构建或联网安装验证）
- [x] 镜像与代理修复
- [x] 在线版本目录与最新版本发现
- [x] 原子切换和失败回滚
- [x] 项目环境、离线安装、临时终端和诊断

### B08 实际修改

- 插件用户名称改为“开发环境管理”，版本更新为 `0.12.0-beta.1`，升级到 Manifest/API v3；市场名称和说明同步更新。
- 下载源补充 `npmmirror`，并新增“镜像失败后使用官方源”开关，默认关闭。关闭时只保留所选源的真实 URL；所选源不支持当前工具会明确报错，不再静默使用官方下载。
- 在线制品缓存键加入镜像名称，切换镜像后不会复用上一下载源的地址。
- 在线 Node、Go、Java、Ruby、Zig、Deno、Bun 的发布资产不再因缺少 SHA-256/digest 字段而从版本目录消失；下载过程也不再计算或比较制品哈希。
- 修正版本切换根因：`current` 现在统一指向版本根目录，`runtime/go/jdk` 等内部目录只由 shim 和版本探针追加一次，避免产生 `runtime/runtime` 等无效路径。
- 当前版本链接采用“新链接预建 → 旧链接暂存 → 新链接启用”的替换流程；启用失败时恢复原链接。
- 环境检测同步适配版本根布局，Node、Go、Java 等切换后从正确的内部可执行文件读取实际版本。
- 增加项目环境工作台：接受工具版本 JSON 清单，显示 active/installed/missing/unsupported 差异，并可按清单一键安装。
- 增加本地 ZIP 离线安装；适用于 Node、Go、Java、PHP、Zig、Deno、Bun 等 ZIP 运行时。需要安装器的 Python、Git、Rust、Ruby 会明确提示不支持 ZIP 导入。
- 增加环境健康报告，集中显示安装根目录、实际镜像、官方回退状态、已安装版本和当前版本。
- 增加临时开发终端，可选择项目目录；仅为该终端会话前置统一 shim PATH，不永久重复写入版本目录。

### B08 执行说明

- 没有执行 SHA-256、哈希校验、质量门禁、构建测试、联网安装、版本切换冒烟或终端启动验证。
- 没有提交或推送 GitHub。

## B09 图片与音视频

- 状态：COMPLETE（beta1 可用范围，未运行媒体样本验证）
- [x] 图片处理
- [x] 动画帧处理
- [x] 轻量音视频处理
- [x] 非破坏项目和导出

### B09 实际修改

- 新增正式插件“图片与音视频” `0.1.0-beta.1`，采用 Manifest v3，市场不再单独发布旧 GIF 插件。
- 图片工作台支持导入、旋转、翻转、灰度、亮度、撤销、还原、缩放宽度、文字水印、质量调整及 PNG/JPEG/WebP 导出。
- 动画时间轴支持多帧导入、逐帧时长、删除、反转和雪碧图导出；非破坏项目可保存编辑步骤和帧清单。
- 音视频工作台支持本地预览、时间定位、视频抽帧以及开始/结束时间范围的 WebM 片段导出。
- 旧“GIF 编辑器”进入兼容期，保留成熟 GIF 时间轴和旧项目导出，不再出现在正式市场目录。
- beta1 未加入复杂多轨剪辑；背景移除、超分和专业转码仍作为可选后续能力，不虚报已完成。

## B10 数据与接口工具

- 状态：COMPLETE（beta1 可用范围，未运行接口联调）
- [x] 结构化数据工具
- [x] 文本与编码工具
- [x] 差异、查询和 API 调试工作区

### B10 实际修改

- 新增正式插件“数据与接口工具” `0.1.0-beta.1`，合并旧 JSON/文本工具的定位。
- 支持 JSON 格式化/压缩、常见 JSON5 清理、CSV/XML/YAML/TOML 简表转 JSON、键值文本导出及双文档合并。
- 支持 Base64、URL、十六进制、UUID、时间戳、JWT、正则、点路径查询和逐行差异。
- SHA-256 只作为用户主动点击的数据工具存在，不参与宿主下载、安装、迁移或发布门禁。
- 接口调试支持 GET/POST/PUT/PATCH/DELETE/HEAD、请求头、请求体及响应状态/响应头/正文查看，可直接发送 GraphQL HTTP 请求。
- 不实现专业压力测试；WebSocket、请求集合和受限脚本留待正式版扩展。

## B11 笔记与剪贴板

- 状态：COMPLETE（beta1 可用范围，未运行交互验证）
- [x] 笔记与日记统一模型
- [x] 剪贴板收集与转笔记
- [x] 随机决策
- [x] 常用换算

### B11 实际修改

- 新增正式插件“笔记与效率” `0.1.0-beta.1`，统一笔记、日记、剪贴板、随机决策和常用换算。
- 笔记支持日期、标签、全文筛选、Markdown/公式文本、`[[双向链接]]` 统计、版本快照、回收站恢复和 JSON 备份。
- 剪贴板支持主动收集、重复内容合并、置顶、复制、清空、隐私暂停和一键转为笔记；默认最多保留 500 条。
- 随机决策支持权重抽取、随机排序和分组，并明确不用于赌博、法律抽签或投资。
- 换算支持公里/英里、摄氏/华氏、千克/磅和百分比；旧汇率插件在兼容期保留历史收藏和查询入口。
- 附件、系统级自动监听排除名单、本地加密和更多联网汇率源留待正式版扩展，不虚报已完成。

## B12 数据迁移

- 状态：COMPLETE（代码完成，未运行迁移测试）
- [x] 迁移映射和事务
- [x] 重复运行及失败回滚语义
- [x] 旧插件只读兼容入口

### B12 实际修改

- 数据库 schema 升级为 v5，新增 `plugin_consolidation_migrations` 记录目标、来源、复制数量和完成时间。
- 新插件首次安装或升级时执行映射：`archive-extractor → document-engine`、`gif-editor → media-toolkit`、`json-toolkit → developer-toolkit`、`diary/clipboard-manager/dice-roller/turntable/exchange-rates → productivity-toolkit`。
- 所有旧键同时以 `legacy:<旧插件名>:<key>` 写入新命名空间，旧命名空间不删除；同一来源完成后重复执行直接复用记录。
- 日记条目额外转换为新 `notes` 模型；剪贴板 `history` 转为新 `clips` 模型并转换时间字段。
- `theme-manager/system-info → 宿主` 的旧数据快照写入宿主 settings 的 `legacyPluginData:*` 键。
- 迁移与插件安装共用事务边界：迁移失败会回滚本轮数据库复制；新装失败会删除目标插件及迁移标记，之后可重新执行。
- 旧日记、剪贴板和转盘后端停止新增持久化写入；旧插件名称统一标记为只读/兼容入口并从正式市场目录移除。

### B09-B12 执行说明

- 没有执行 SHA-256、哈希校验、质量门禁、构建测试、媒体样本、接口联调、迁移测试或冒烟测试。
- 没有提交或推送 GitHub。

## B13 全量验收与发布准备

- 状态：COMPLETE（本地验收完成；按用户要求不打包、不发布）
- [x] Rust 格式、Clippy、workspace tests
- [x] Tauri 前端生产构建
- [x] 五个正式插件独立 typecheck/test/build
- [x] 宿主 lint、TypeScript 类型检查、宿主测试和全部插件测试
- [x] 三份真实 PDF 的 OCR、原生解析、结构、导出和 RAG 重点验收
- [x] schema v5、Manifest v3、镜像源断言和旧插件只读兼容测试同步
- [ ] 2.0.1 已安装程序原位升级（不生成安装包，本轮以数据库迁移和兼容测试代替）
- [ ] Windows 安装包验收（用户要求先不要发布，未生成安装包）
- [ ] beta 更新资产、签名和发布（明确暂停）

### B13 OCR 与文件处理实测

- `fogharbor_botanical_field_notes_scanned.pdf`：10 页扫描 PDF，真实 OCR 全链路耗时 79.21 秒；214 个 OCR 文本块、28 个标题、5 个 RAG 分块，`ragQuality=good`，无质量标记；六个预期中文植物名称全部识别成功。
- `linear algebra by strang 4 th edition.pdf`：541 页原生文字教材；61,167 个原生文本块、5,954 个公式块、56 个矩阵块、549 个 RAG 分块；章节树一致性 1.0；Markdown 1,232,653 字节、DOCX 622,606 字节且 XML 结构通过。公式不确定率约 7.77%，因此结果正确标记为 `ragQuality=degraded`，未伪装成满分。
- `78_361002200708112019_24.pdf`：1 页中文业务 PDF，原生文字路线；51 个文本块、1 个 RAG 分块，质量为 `good`，无非法控制字符。
- 三份样本均渲染代表页进行视觉核对：扫描封面、教材公式页和中文表格业务页均可读，解析路线与页面实际内容一致。
- OCR Worker：11 项测试通过；ONNX Runtime、PP-OCRv6 检测和 PP-OCRv5 中英文识别模型从本地目录成功加载。

### B13 自动验收结果

- Rust：`cargo fmt --check` 与 Clippy 零警告；workspace 252 项通过、0 失败、4 项本地样本测试默认忽略，其中 OCR、教材和普通业务 PDF 已单独带样本执行通过。
- 宿主：ESLint 通过；TypeScript 全工作区类型检查通过；宿主 110 项测试通过。
- 插件：全部插件测试通过；五个正式插件分别完成类型检查、测试和生产构建。
- 前端：Tauri 前端生产构建通过。主共享包仍产生约 619.58 kB 的 Vite 体积提示，不影响构建，但应在正式版继续拆分。
- 修复验收中发现的问题：Cargo lock 不同步、schema v5 测试仍断言 v4、Manifest v3 测试仍拒绝 v3、UniEnv Node 镜像数量断言过期、旧日记/转盘测试仍假定可写、PluginContext v3 测试桩缺字段、三个新插件缺少测试入口。

### B13 发布状态

- 未生成 Windows 安装包、beta 更新资产、签名、校验文件或发布制品。
- 未提交、未推送 GitHub，未创建 tag 或 Release。
- 没有新增 SHA-256、哈希门禁或防御性框架；本轮运行的是功能验收与编译测试。
