[package]
name = "poc-backend-sidecar"
version = "0.1.0"
edition = "2021"
description = "PoC: quickjs-ng sidecar for plugin backends (P2 gate)"

[dependencies]
rquickjs = { version = "0.12", features = ["futures"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
codegen-units = 1
