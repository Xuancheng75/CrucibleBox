[package]
name = "ocr-worker"
version = "0.1.0"
edition = "2021"

[dependencies]
ort = { version = "2.0.0-rc.13", default-features = false, features = ["std", "ndarray", "tracing", "api-27", "directml", "load-dynamic"] }
image = "0.25"
ndarray = "0.16"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
